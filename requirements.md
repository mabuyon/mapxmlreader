# 要件定義書

**プロジェクト名:** 地籍図XML・標高データ → DXF変換アプリ  
**作成日:** 2026-05-30  
**バージョン:** 1.1

---

## 1. プロジェクト概要

### 1.1 目的
法務省の地籍図XML、および国土地理院の標高データを、JW-CADで扱える DXF R12形式のCADファイルに変換するブラウザ完結型ツール。

### 1.2 動作環境
| 項目 | 要件 |
|---|---|
| 動作方式 | ブラウザ完結型（サーバー不要） |
| 形態 | 単一HTMLファイル（`index_standalone.html`） |
| 対象ブラウザ | Chrome 90+ / Edge 90+ / Firefox 84+ / Safari 15.4+ |
| ネットワーク | 地籍図変換：不要 / 標高タイルDL時：必要 |
| 出力形式 | DXF R12（AC1009）JW-CAD対応 |

---

## 2. 機能一覧

| 機能ID | 機能名 | 区分 |
|---|---|---|
| F-01 | 地籍図XML → DXF変換 | 既存（変更なし） |
| F-02 | 標高データ → DXF変換 | 新規追加 |

---

## 3. F-01：地籍図XML → DXF変換（既存機能・変更なし）

### 3.1 入力
- 法務省地籍図XML（`.xml`）または ZIP圧縮ファイル（`.zip`）

### 3.2 処理
- XMLをブラウザのDOMParserでパース
- 7種類のデータ抽出（点・曲線・面・筆界線・筆界点・基準点・地番・図郭）
- 公共座標系（X=北/Y=東）→ DXF座標系（X=東/Y=北）に変換、m→mm スケール変換

### 3.3 出力DXFレイヤー
| レイヤー名 | 内容 | 色 | エンティティ |
|---|---|---|---|
| BOUNDARY | 筆界線 | 赤(1) | LINE |
| LANDNUMBER | 地番 | 緑(3) | TEXT |
| BND_POINT | 筆界点 | 青(5) | POINT |
| REF_POINT | 基準点 | 黄(2) | POINT + TEXT |
| FRAME | 図郭 | 白(7) | LINE + TEXT |

---

## 4. F-02：標高データ → DXF変換（新規機能）

### 4.1 UI構成

#### タブ切り替え
- 既存機能と新機能を**タブ**で切り替え
  - タブ1：地籍図XML → DXF（既存）
  - タブ2：標高データ → DXF（新規）

#### 標高タブの操作フロー
```
Step 1: 変換範囲の決定
   ↓
Step 2: メッシュ解像度の選択
   ↓
Step 3: データソースの選択・取得
   ↓
Step 4: DL前確認（タイルAPIの場合）
   ↓
Step 5: 変換設定（等高線間隔等）
   ↓
Step 6: 変換・DXFダウンロード
```

---

### 4.2 Step 1：変換範囲の決定

| 方式 | 内容 |
|---|---|
| XMLから自動引き継ぎ | 「地籍図XML → DXF」タブでXMLを読み込み済みの場合、「XMLの範囲を引き継ぐ」ボタンで自動入力 |
| 緯度経度を手入力 | 最小緯度・最大緯度・最小経度・最大経度を数値で入力（WGS84、十進度） |

**公共座標系の系番号**
- XMLから自動検出する
- 検出できない場合はプルダウン（1〜19）で手入力を求める

---

### 4.3 Step 2：メッシュ解像度の選択

| 選択肢 | タイル種別 | 分解能 | 提供範囲 |
|---|---|---|---|
| 1mメッシュ | `dem1a_png` | 約1m/pixel | 一部地域（航空レーザー測量エリア） |
| 5mメッシュ（航空レーザー） | `dem5a_png` | 約5m/pixel | 広域 |
| 5mメッシュ（写真測量） | `dem5b_png` | 約5m/pixel | 広域 |
| 5mメッシュ（地形図） | `dem5c_png` | 約5m/pixel | 広域 |

**フォールバック仕様**
- 1mメッシュを選択した際に、対象範囲にデータが存在しない場合は警告を表示し、5mメッシュへの切り替えをユーザーに提示する

---

### 4.4 Step 3：データソースの選択

#### Option A：地理院タイルAPI（自動DL）
- 国土地理院の標高タイル（PNG形式）をブラウザから直接取得
- ベースURL: `https://cyberjapandata.gsi.go.jp/xyz/{tiletype}/{z}/{x}/{y}.png`
- CORS対応済みのため、ブラウザから直接GETリクエスト可能
- 標高値の抽出式：
  ```
  x = R×65536 + G×256 + B
  if x >= 8388608: x -= 16777216
  elevation = x × 0.01  [m]
  R=128, G=0, B=0 の場合: データなし（海・欠損）
  ```

#### Option B：基盤地図情報DEMファイル（手動UP）
- 国土地理院サイトからDLしたZIP/XMLをアップロード
- 既存アプリと同様のドラッグ&ドロップUI
- 対応フォーマット：基盤地図情報数値標高モデル XML形式

---

### 4.5 Step 4：DL前確認（タイルAPIの場合のみ）

変換範囲・ズームレベルから必要タイル数と概算データ量を計算し、以下の情報を表示してから実行：

| 表示項目 | 内容 |
|---|---|
| 必要タイル数 | 縦×横のタイル数 |
| 概算DL容量 | タイル数 × 平均タイルサイズ |
| 推定処理時間 | 参考値 |
| 対象メッシュ種別 | 選択中のタイル種別 |

「ダウンロード開始」ボタンで実行（取消可能）。

---

### 4.6 Step 5：変換設定

| 設定項目 | 選択肢 | デフォルト |
|---|---|---|
| 等高線間隔（主曲線） | 0.5m / 1m / 2m / 5m / 10m | 1m |
| 計曲線の倍率 | 主曲線×5 | 固定 |
| 標高点の出力 | する / しない | する |
| 標高点の間引き間隔 | 5m / 10m / 20m / 50m | 10m |

---

### 4.7 Step 6：DXF出力

#### 出力DXFレイヤー
| レイヤー名 | 内容 | 色 | エンティティ |
|---|---|---|---|
| CONTOUR_MAJOR | 計曲線（主曲線×5間隔） | シアン(4) | LINE |
| CONTOUR_MINOR | 主曲線 | 青(5) | LINE |
| ELEV_POINT | 標高点 | 黄(2) | POINT + TEXT |

#### 座標系
- 出力座標：公共座標系（mm単位）
- F-01の地籍図DXFと**同一座標空間**に出力し、重ね合わせ可能にする
- DXF形式：R12（AC1009）、JW-CAD互換

#### 等高線の生成方法
- **Marching Squares アルゴリズム**をJavaScriptで実装
- 線分はLINEエンティティで出力（LWPOLYLINEは使用しない：JW-CAD非対応のため）
- データなし領域（海・欠損）はスキップ

---

## 5. 非機能要件

| 項目 | 要件 |
|---|---|
| サーバーレス | すべての処理をブラウザ内で完結。外部サーバーへのデータ送信なし（タイル取得APIを除く） |
| ファイル構成 | 単一HTMLファイルで完結（追加ファイル・インストール不要） |
| プログレス表示 | 処理中はステップごとにプログレスバーと状態メッセージを表示 |
| エラーハンドリング | タイル取得失敗・データなし・変換失敗の各エラーを日本語でユーザーに通知 |
| パフォーマンス | 50タイル程度の取得・変換を30秒以内で完了することを目標 |
| JW-CAD互換 | 出力DXFはJW-CADで正常に開けること |

---

## 6. 外部依存

| ライブラリ | バージョン | 用途 | 既存/新規 |
|---|---|---|---|
| JSZip | 3.10.1 | ZIP展開 | 既存 |
| 地理院タイルAPI | - | 標高タイル取得 | 新規 |

---

## 7. 対象外（スコープ外）

- 地図UI上での範囲選択（マウスドラッグ等）
- 3D DXF出力（Z座標付き）
- 10mメッシュ（dem10b）への対応
- オフライン動作（標高タイル取得機能）
- スマートフォン対応

---

## 8. 用語定義

| 用語 | 説明 |
|---|---|
| 主曲線 | 設定間隔ごとの等高線 |
| 計曲線 | 主曲線の5本ごとに描く太い等高線 |
| 公共座標系 | 日本測量法に基づく平面直角座標系（19系）。JGD2000基準、ガウス・クリューゲル投影 |
| WGS84 | GPSで使用される世界測地系。緯度経度表現 |
| DEM | Digital Elevation Model（数値標高モデル） |
| 地理院タイル | 国土地理院が公開するXYZタイル形式の地図・地理情報データ |
| Marching Squares | グリッドデータから等値線（等高線）を生成するアルゴリズム |
